const abi = window.__APP_METADATA__.abi;

export const erc20Abi = abi.erc20;
export const voucherAbi = abi.voucher;
export const charityAbi = abi.charity;
export const xbitAbi = abi.xbit;
export const sortesAbi = abi.sortes;
export const coordinatorAbi = abi.coordinator;
export const xexpAbi = abi.xexp;
export const xlpAbi = abi.xlp;
export const lockerAbi = abi.locker;
